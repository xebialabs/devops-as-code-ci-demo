pipeline {

    agent any

    stages {
        stage("Apply XLD") {
            steps {
               echo 'Applying XLD'

               sh '/tmp/xl apply -v -f yaml/xld.yaml --xl-deploy-url http://xld:4516/ --xl-deploy-username admin --xl-deploy-password admin'
            }
        }
    }

}
